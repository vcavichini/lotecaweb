{
  "name": "newloteca-node",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "start": "bun run src/server.ts",
    "test": "bun test",
    "build": "bun x tsc --noEmit",
    "checker": "bun run scripts/loteca-checker.ts",
    "verify:runtime": "bun --version"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "hono": "^4.12.16"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "5.8.3"
  }
}
